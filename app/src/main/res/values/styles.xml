<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.DeviceDefault">
        <!-- Customize your theme here. -->
    </style>
    <style name="MainPage" parent="android:Theme.DeviceDefault.Light.NoActionBar">
        <item name="android:background">@android:color/background_light</item>
    </style>

    <style name="test" parent="Theme.AppCompat.Light.NoActionBar" >
        <item name="android:colorBackground">@android:color/white</item>
        <item name="android:colorPrimary">#448aff</item>
        <item name="android:colorPrimaryDark">@color/primary_dark_device_default_dark</item>
        <item name="android:colorAccent">@color/accent_device_default_dark</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorPrimaryInverse">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:statusBarColor">@color/backgroundAndMenu</item>
        <item name="android:textColorHint">@android:color/darker_gray</item>
    </style>

    <style name="LussisTheme" parent="AppTheme" >
        <item name="android:colorPrimary">@color/backgroundAndMenu</item>
        <item name="android:colorBackground">@android:color/background_light</item>
        <item name="android:statusBarColor">@color/backgroundAndMenu</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@color/login_text_color</item>
        <item name="android:colorForeground">@color/accent_device_default_dark</item>
    </style>

</resources>
